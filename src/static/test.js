import * as yup from 'yup';

export const test = [
    {
        label: "Chuỗi",
        name:"string",
        type:"text",
        input: "basic"
    }
]

export const schema = yup.object().shape({
});